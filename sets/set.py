# unordered by default, no duplicates(hashset)
# can't change items, can only add or delete
# true false, 1 and 0 pairs are treated as duplicates resepectively
# can be of mixed data types 
fruits = {"apple", "banana", "cherry"}
print(fruits)

print(len(fruits))
print(type(fruits))

# can create a set using the built in constructor
fruits2 = set(("apple", "banana", "cherry"))
